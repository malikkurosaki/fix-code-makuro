saat cmd+shift+p akan muncul popup input box, saat saya ketik fix code akan muncul opsi fix code
ada set api key dan reset api key , tambahkan lagi opsi untuk memilih model default 
bisa search model dari open router secara otomatis dan dapat data model terbaru
lalu bisa memilih model dari list model yang ada